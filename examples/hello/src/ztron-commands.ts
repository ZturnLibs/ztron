/**
 * Generated by `ztron codegen`. Do not edit by hand.
 * Run `ztron codegen` after adding/changing commands.
 */
import { invoke as rawInvoke } from "@ztron/api";
import type { InvokeOptions } from "@ztron/api";

export type KnownCommands = {
    "my:greet": { args: { name: string }; result: string }
    "my:add": { args: { a: number; b: number }; result: number }
    "my:echo": { args: { msg?: string }; result: string }
};

/** Type-safe invoke: cmd must be a known command, args/result inferred from its declaration. */
export function invoke<C extends keyof KnownCommands>(
  cmd: C,
  args: KnownCommands[C]["args"],
  options?: InvokeOptions,
): Promise<KnownCommands[C]["result"]>;

export async function invoke(
  cmd: string,
  args?: unknown,
  options?: InvokeOptions,
): Promise<unknown> {
  if (cmd === "my:greet") return rawInvoke(cmd, args as never, options);
  if (cmd === "my:add") return rawInvoke(cmd, args as never, options);
  if (cmd === "my:echo") return rawInvoke(cmd, args as never, options);
  return rawInvoke(cmd, (args ?? {}) as never, options);
}
export default invoke;

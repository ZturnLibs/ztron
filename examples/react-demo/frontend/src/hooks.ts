/**
 * Ztron React hooks — now the official `@zturnlibs/ztron-react` package.
 *
 * Re-export shim kept so the demo's imports stay stable; the implementation
 * lives in packages/ztron-react (same names/signatures, StrictMode-safe
 * cleanups), and this demo is its integration proof on the real pipeline.
 */
export * from "@zturnlibs/ztron-react";

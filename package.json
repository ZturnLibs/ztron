{
  "name": "ztron",
  "version": "0.1.0",
  "private": true,
  "description": "A Tauri-style cross-platform desktop framework rewritten in TypeScript on top of txiki.js + system WebView",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @ztron/cli dev --root ../../examples/hello"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
